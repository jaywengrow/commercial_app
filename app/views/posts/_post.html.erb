<span>Post #<%= "#{post.id}" %>:</span>
<span>Title: <%= "#{post.title}" %>:</span>
<div><%= raw post.embed_text %></div>
<% if current_user == post.user %>	
	<%= link_to "Edit", edit_post_path(post) %>
	<%= link_to "Delete", post, :method => :delete, :confirm => "Are you sure?" %>
<% end %>
<br /><br />
